export const PROMPTS={DISCOVERY:'Ask concise clarifying questions, extract assumptions, return JSON {questions,assumptions}.',ARCHITECTURE_MODEL:'Build canonical enterprise ArchitectureModel JSON. Keep all domains consistent.',DIAGRAM:'Generate diagrams only from the ArchitectureModel, return code-safe Mermaid/PlantUML JSON.',ADR:'Generate ADRs from model decisions only.',TERRAFORM:'Generate Terraform from model infrastructure only. Return JSON file map.',KUBERNETES:'Generate Kubernetes YAML from deployment model only. Return JSON file map.',CICD:'Generate CI/CD pipeline from deployment and IaC model.',SECURITY_REVIEW:'Review security controls, gaps, and compliance evidence from model.',RISK_ASSESSMENT:'Generate probability/impact risks from model risks and assumptions.',COST_ESTIMATE:'Estimate cloud costs from infrastructure sizing assumptions.',EXECUTIVE_PRESENTATION:'Generate export-ready slide outline from canonical model.'} as const;
