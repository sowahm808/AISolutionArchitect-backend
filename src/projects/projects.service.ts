import { ForbiddenException,Injectable,NotFoundException } from '@nestjs/common';import { randomUUID } from 'crypto';import { PrismaService } from '../prisma/prisma.service';import { AuthUser } from '../common/current-user.decorator';import { CreateProjectDto,UpdateProjectDto } from './dto';@Injectable()export class ProjectsService{constructor(private db:PrismaService){}create(u:AuthUser,d:CreateProjectDto){return this.db.project.create({data:{id:randomUUID(),...d,organizationId:u.organizationId,ownerId:u.sub}})}findAll(u:AuthUser){return this.db.project.findMany({where:{organizationId:u.organizationId},orderBy:{updatedAt:'desc'}})}async findOne(u:AuthUser,id:string){const p=await this.db.project.findFirst({where:{id,organizationId:u.organizationId}});if(!p)throw new NotFoundException('Project not found');return p}async update(u:AuthUser,id:string,d:UpdateProjectDto){await this.findOne(u,id);return this.db.project.update({where:{id},data:d})}async remove(u:AuthUser,id:string){if(['REVIEWER','STAKEHOLDER'].includes(u.role))throw new ForbiddenException('Read-only role');await this.findOne(u,id);return this.db.project.delete({where:{id}})}}
