import { PrismaClient } from '@prisma/client';import * as argon2 from 'argon2';import { randomUUID } from 'crypto';const db=new PrismaClient();async function main(){const org=await db.organization.upsert({where:{id:'demo-org'},update:{},create:{id:'demo-org',name:'Contoso Architecture',industry:'Financial Services',plan:'ENTERPRISE'}});const user=await db.user.upsert({where:{email:'demo@aisa.local'},update:{},create:{id:'demo-user',email:'demo@aisa.local',displayName:'Demo Architect',passwordHash:await argon2.hash('ChangeMe123!'),role:'ADMIN',organizationId:org.id}});await db.project.upsert({where:{id:'demo-project'},update:{},create:{id:'demo-project',organizationId:org.id,ownerId:user.id,name:'Legacy .NET to Azure Migration',description:'Migrate .NET Framework portfolio to Azure landing zone.',cloudProvider:'Azure',migrationType:'Rehost/Replatform',currentStateSummary:'On-prem IIS and SQL Server.',targetStateSummary:'Azure App Service, AKS, Azure SQL, and DevSecOps pipelines.'}})}main().finally(()=>db.$disconnect());
